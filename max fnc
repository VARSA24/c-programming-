#include<stdio.h>
int max(int a, int b)
{
    if(a > b)
        return a;
    else
        return b;
}
int main()
{
    int x, y, result;
    scanf("%d %d", &x, &y);
    result = max(x, y);   
    printf("Maximum number is %d", result);
    return 0;
}

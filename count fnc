#include<stdio.h>
int countDigits(int n)
{
    int count = 0;
    while(n != 0)
    {
        count++;
        n = n / 10;
    }
    return count;
}
int main()
{
    int x, result;
    scanf("%d", &x);
    result = countDigits(x);
    printf("Number of digits = %d", result);
    return 0;
}

#include<stdio.h>
int main()
{
    int x, digit, sum = 0;
    scanf("%d", &x);
    while(x != 0)
    {
        digit = x % 10;
        sum = sum + digit;
        x = x / 10;
    }
    printf("Output : %d", sum);
    return 0;
}

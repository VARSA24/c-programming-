#include<stdio.h>
int main()
{
    int n, a[50], min;
    scanf("%d", &n);
    for(int i = 0; i < n; i++)
    {
        scanf("%d", &a[i]);
    }
    min = a[0];
    for(int i = 1; i < n; i++)
    {
        if(a[i] < min)
            min = a[i];
    }
    printf("Min = %d", min);
    return 0;
}

#include<stdio.h>
int checknum(int a)
{
    if(a % 2 == 0)
        return 1;
    else
        return 0;
}
int main()
{
    int x, result;
    scanf("%d", &x);
    result = checknum(x);
    if(result == 1)
        printf("Even");
    else
        printf("Odd");
    return 0;
}

#include<stdio.h>
int main()
{
    int x, digit, rev = 0;
    scanf("%d", &x);
    while(x != 0)
    {
        digit = x % 10;
        rev = rev * 10 + digit;
        x = x / 10;
    }
    printf("Output : %d", rev);
    return 0;
}

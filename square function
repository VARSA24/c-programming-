#include <stdio.h>
int square(int n)
{
    return n * n;
}
int main()
{
    int x, result;
    scanf("%d", &x);
    result = square(x);   // function call
    printf("Square = %d", result);
    return 0;
}
